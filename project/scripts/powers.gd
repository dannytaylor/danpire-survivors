extends Resource
class_name power_text

var powers = {
	'dog': '+X orbiting dogs, Y damage',
	'mustard': 'shoot mustard every X sec, Y damage',
	'ketchup': 'drop a ketchup package every X sec, Y damage when triggered',
	'dash': 'quick dash on X sec recharge'
}

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
